.venv/
__pycache__/
*.pyc
*.log
/tmp/
# 監査ツール・launchd・ローカル運用の生成物（コミット対象外）
audit_state.json
audit_report.md
run_daily.sh
launchd.err.log
launchd.out.log
# DBはGitHub Actions(--full)で生成しReleaseに置く。gitには入れない（数十MB）。
denki_bid.db
denki_bid.db.gz

# APIキー等の秘密（絶対にコミットしない）
.env
.env.*
*.key
