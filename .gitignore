.venv/
__pycache__/
*.pyc
*.log
/tmp/
# 監査ツール・launchd・ローカル運用の生成物（コミット対象外）
audit_state.json
audit_report.md
run_daily.sh
launchd.err.log
launchd.out.log
# DBはGitHub Actions(--full)で生成しReleaseに置く。gitには入れない（数十MB）。
denki_bid.db
denki_bid.db.gz

# APIキー等の秘密（絶対にコミットしない）
.env
.env.*
*.key

# 認証DB（パスワードハッシュ。コミットしない）
users.db
set_render_env.sh

# ローカル作業用の生成物・別プロジェクト（コミット対象外）
公告PDF/
newscreen/
応募ターゲット_*.csv
応募準備_*.md
